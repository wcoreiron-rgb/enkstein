# Enkstein Maturity Matrix (2026)

**Date:** 2026-08-07
**Release:** 0.8.2
**Purpose:** Public, conservative status tracking for platform security/runtime maturity.

Status legend:
- **Shipped**: in mainline runtime with verifiable behavior.
- **In Progress**: partially implemented or feature-flagged; not complete.
- **Planned**: scoped, not yet implemented.

| Capability Area | Status | Current Evidence | Gaps to Close |
|---|---|---|---|
| Cryptographic agent identity mesh | In Progress | Ed25519 inter-agent signing + verify endpoint + remote-agent signed enrollment tokens + public-key fingerprint tracking + key rotation endpoint | Full attestation mesh (SPIFFE-like), hardware-backed attestation, and production key lifecycle policy |
| Hard execution isolation model | In Progress | Ring policy + Trust Fabric ring decisions + route-level convergence in exec/remediation + fail-closed execution/approval behavior when Trust Fabric unavailable | Full OS sandbox guarantees across all execution channels |
| Formal SRE governance layer | In Progress | Error-budget + circuit-breaker primitives, SRE API/status endpoints | Published SLO docs, error-budget policy packs, richer telemetry/export |
| OWASP Agentic Top 10 evidence mapping | In Progress | Dedicated ASI mapping markdown + linked controls | Per-category adversarial tests and deeper evidence anchors |
| Inter-agent secure messaging (prod default) | In Progress | Feature-flagged signed secure channel in swarm task paths + verify endpoint | Default-on rollout + key governance + replay resistance policy |
| Policy test harness strength | In Progress | Ring tests + trust-fabric regressions + policy-pack allow/deny + replay regressions | Chaos/replay expansion and CI policy gates tied to policy diffs |
| Multi-tenant hardening proof | In Progress | Dedicated per-surface isolation suites — `test_tenant_isolation.py`, `test_cross_tenant_access.py`, `test_finding_tenant_ingestion.py`, `test_audit_tenant_scope.py`, `test_tenant_memory_scope.py`, `test_control_evidence_tenant_scope.py`, `test_channel_exec_tenant_scope.py` — covering tenant-scoped identity inventory, connectors, finding ingestion, audit records, control evaluation/remediation, Swarm, channel, and execution paths. Memory Cortex injects only short redacted context and blocks secret/prompt-injection-like memory writes. Remaining unenforced paths stay marked `xfail` with documented reasons rather than being claimed as covered. | PostgreSQL RLS and complete route-level adversarial coverage |
| Connector trust/provenance verification | In Progress | Connector Health separates configured, approved, and recorded provider verification; production scans default to live evidence only; shared scan/Swarm provenance blocks demo data from control verdicts and remediation | Live credential tests against each tenant's vendor APIs, signed provenance chain, and complete adapter conformance coverage |
| Command/channel control plane convergence | In Progress | Command remote-agent routes + signed enrollment token issuance + remote-agent key rotation + capability inventory + channel ingress normalized to unified command contract (`POST /api/v1/commands`) with policy outcome propagation, simulate-path parity, remote dispatch guardrails (tenant/kill-switch/intent allowlist), webhook/email/CLI ingress adapters, command pending/approve/reject/bulk-review/timeline/status/approval-policy endpoints, frontend pending-command approve/reject UX + timeline/status views + source/risk filters + approval-threshold controls + multi-select bulk actions, persisted multi-operator approval state (self/duplicate guards + approvals progress), chat-ops review verbs (`approve <command_id>` / `reject <command_id>`) routed through the same governed approval APIs, Slack/Teams outbound response delivery through configured channel webhooks with persisted `response_sent` state, Slack thread metadata, Slack Block Kit / Teams MessageCard approval action metadata, outbound card/detail visibility, and Control Center v2 summary backend (`GET /api/v1/dashboard/control-center-summary`) powering unified operator cockpit cards including outbound reply health | Expand policy-state durability beyond event metadata, add signed/native channel app interactions beyond incoming-webhook card metadata, and add richer approval delegation controls |
| Operator-grade executive reporting | In Progress | Trust Fabric dashboard + probes + status panels + Swarm live event stream + Swarm ticket draft + compliance impact rollup + Create Ticket handoff with strict remediation payload guardrails (`provider=jira`, `target_type=ticket`, key/length validation) + Trust Fabric-governed Compliance Assurance evidence export with framework rollups and SHA-256 chain-of-custody hash | Executive risk rollups linked to broader evidence/compliance controls across more Capability Nodes and polished PDF/export UX |
| Swarm runtime maturity | In Progress | Bounded parallel execution + real `/task` routing for Identity/Cloud/Threat/Arc/Access/Data/Dev/Endpoint/App/Log/Net/Compliance/Intel/Recovery/SaaS/Privacy/User/Insider/Vendor/AttackPath/Automation/Config/Exposure/Custom + SSE stream + connector-backed task paths for Identity/Entra, Cloud, Endpoint, and Dev + standardized connector-state/data-source metadata surfaced across all current swarm-routed claw task outputs + trigger/schedule-driven swarm launches (`START_SWARM`/`FIRE_SWARM`/`SWARM_JOB`) + pre-execution approval gating and approve-to-run flow + Sprint 6 suspicious-identity preset workflow + Microsoft identity incident preset workflow + remediation ticket handoff tests | Broaden connector-backed execution across additional claw providers and reduce simulation fallback usage in provider adapters |
| Model routing maturity (Model Cortex) | In Progress | Governed Local/Hybrid/Cloud routing; effective request/conversation/Project/artifact classification with tested restricted-data local pinning; live CLI readiness; root-scoped Codex App Server Cowork sessions with governed operations and scanned/redacted output; explicit bounded specialist task graph with validated workspace/requester attribution, peer-message policy, ordered fallback, timeout/dependency skipping, and actual adapter-attempt provenance; tenant-scoped profile/call filtering | Provider-side graph cancellation, persisted native agent output/patch/test artifacts, per-conversation/Project settings, tenant-owned persisted role presets, hardware-aware local recommendations/confirmed pulls, DB-backed profiles, Gemini CLI, DeepSeek/Moonshot/custom adapters, and native Model Cortex Azure support |
| Memory Cortex runtime integration | In Progress | Swarm task input gets safe redacted memory context; task outputs expose `memory_context_loaded`; high-risk Swarm Judge results create proposed incident memory entries after safety scan; proposal queue supports approve/reject endpoints; incident memory rollback marks excluded memory with audit timeline; Memory UI exposes proposal review and rollback controls | Add richer version history/diff UI, tenant-scoped memory tables, and scheduled memory consolidation review |
| Release/deployment governance | In Progress | Release Governance API/UI ships a universal deployment preflight contract for GitHub Actions, GitLab CI, Jenkins, Azure DevOps, ArgoCD, Terraform Cloud, cloud SDK/CLI, Kubernetes/Helm/Docker, Bash/PowerShell/Python/Node/Ansible, full-stack releases, and AI service stacks. Preflight runs through Trust Fabric, blocks unsafe plans, rejects self-approval, returns execution handoff metadata, exposes `/task`, and generates SHA-256 evidence bundles. | Persist deployment gates in DB, add real CI/CD execution adapters, attach deployment evidence to compliance-control tables, and add native runner callbacks for execution status |
| Terraform/IaC governance | In Progress | Terraform Governance ships FastAPI/UI surfaces for natural-language Terraform module build, Terraform HCL review, agent-style secure Terraform generation, Terraform plan risk analysis, seeded/persisted findings, provider status for Terraform MCP/Terraform Cloud/tfsec/Checkov/Infracost, normalized 0-100 risk scores, `/build`, `/generate`, `/scan`, `/task`, connector registry mapping, and Swarm dispatcher routing. Generate responses now include MCP trace, module artifacts, applied controls, GCP Cloud SQL support, and Review/Plan handoff metadata. Build requests are AI Security-scanned before generation, and review/generate/plan/build actions are Trust Fabric-gated with CIS, NIST, SOC 2, ISO 27001, PCI-DSS, and OWASP sensitive-data mappings where applicable. | Add deeper live provider-backed ingestion for Terraform Cloud/checkov/tfsec/Infracost, persisted review/plan/build history, CI runner callbacks, and attach Terraform Governance evidence directly to deployment gates |
| Plexus peer messaging | In Progress | Governed peer messages between Capability Nodes via `POST/GET /plexus/messages`, per-node inbox pulls, held-message approval, and verify/acknowledge steps; participant identity is required (403 without it) and message metadata is verified on read. Covered by `test_marcellus_runtime.py`. | Default-on encrypted transport across every node pair, replay-resistance policy, and key lifecycle governance |
| Reflex execution (policy-bounded local autonomy) | In Progress | `ReflexDefinition`/`ReflexExecution` models with registration, event evaluation (`POST /reflexes/evaluate`), and a recorded decision log (`GET /reflexes/executions`) so a Node can act locally inside policy bounds without central dispatch. Covered by `test_marcellus_runtime.py`. | Broader trigger taxonomy, per-Arm reflex budgets, and chaos/replay validation of reflex misfires |
| Regeneration (checkpoint recovery) | In Progress | Signed Node checkpoints (`POST/GET /regeneration/checkpoints`) and governed Regeneration runs (`POST/GET /regeneration/runs`) restoring a Capability Node from a signed checkpoint. Covered by `test_marcellus_runtime.py`. | Automated failure-triggered regeneration, cross-host restore, and checkpoint retention/expiry policy |
| Public maturity transparency | In Progress | This matrix + OWASP split docs, re-verified against passing tests at each release | Keep matrix synced with code and tests each release |
| Production deployment readiness | In Progress | Baseline production deployment guide covering TLS, secrets, database/Redis posture, Trust Fabric fail-closed expectations, CI security, backup/restore checklist, and Release Governance governed preflight/evidence gates | Cloud-specific deployment examples, HA sizing, disaster recovery runbooks, DB-backed Release Governance history, and upgrade playbooks |

---

## Reference Documents

- `docs/owasp-agentic-mapping.md` (LLM Top 10 mapping)
- `docs/owasp-asi-mapping.md` (Agentic ASI Top 10 mapping)
- `backend/app/trust_fabric/enforcement.py`
- `backend/app/services/sre_policy.py`
- `backend/app/services/ring_policy.py`
- `backend/app/core/swarm/orchestrator.py`
- `backend/app/core/swarm/routes.py`
- `backend/app/core/modelclaw/routes.py`
- `backend/app/claws/releaseclaw/routes.py`
- `backend/app/core/marcellus/runtime_routes.py` (Plexus / Reflex / Regeneration)
- `backend/app/core/marcellus/runtime_security.py`
- `backend/app/models/marcellus.py`
- `docs/production-deployment.md`
- `docs/evidence-and-connector-contract.md`
